#include <stdio.h>

int main()
{
  int radius = 5;
  int height = 10;
  printf("The area of circle with radius %d is %f\n", radius, 3.14 * radius * radius);
  printf("The volume of circle with radius %d and height %d is %f", radius, height, 3.14 * radius * radius);
  return 0;
}