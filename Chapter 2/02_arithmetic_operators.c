#include <stdio.h>

int main() {
  int a = 6;
  int b = 3;
  int c = a + b;

  printf("The value of a is %d, b is %d and c is %d\n", a, b, c);

  printf("The remainder when a is divided by b is: %d", a%b);

  return 0;
}